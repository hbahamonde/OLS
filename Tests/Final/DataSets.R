# install.packages("AER")
library(AER)


data("Fatalities") # "fatal" // numeric. Number of vehicle fatalities.
data("HousePrices") # "price" // Sale price of a house.
data("Journals") # "price" // Library subscription price.
data("Guns") # "violent" // violent crime rate (incidents per 100,000 members of the population).
data("TeachingRatings") # b"eauty" // rating of the instructor's physical appearance by a panel of six students, averaged across the six panelists, shifted to have a mean of zero.
